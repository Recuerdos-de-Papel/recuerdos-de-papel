// Base de datos configurada con Prisma
// El cliente de Supabase se usa en el frontend

import { logger } from '../utils/logger';

export const connectDatabase = async () => {
  logger.info('Base de datos lista (usar Prisma)');
};

export default null;